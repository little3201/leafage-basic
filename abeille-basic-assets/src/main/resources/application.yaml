##========server port=======##
server:
  port: 8763

##=====application name=====##
spring:
  application:
    name: abeille-basic-assets

  ##=====active assets=====##
  profiles:
    active: dev

#actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,env,metrics,prometheus