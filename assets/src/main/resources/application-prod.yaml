logging:
  level:
    root: INFO
    org:
      springframework: INFO
      hibernate: INFO

spring:
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/abeille_assets?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC8
    username: root
    password: root