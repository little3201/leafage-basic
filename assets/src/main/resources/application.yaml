server:
  port: 8764

spring:
  application:
    name: assets

  profiles:
    active: dev

  cloud:
    consul:
      discovery:
        heartbeat:
          enabled: true

  jpa:
    open-in-view: false

  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8761