server:
  port: 8764

spring:
  application:
    name: assets
  profiles:
    active: dev
  web:
    resources:
      add-mappings: false

  cloud:
    consul:
      discovery:
        heartbeat:
          enabled: true