server:
  port: 8764

spring:
  application:
    name: assets

  profiles:
    active: dev

  web:
    resources:
      add-mappings: false

management:
  endpoints:
    web:
      exposure:
        include: 'prometheus'
  metrics:
    tags:
      application: ${spring.application.name}