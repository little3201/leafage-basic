spring:
  security:
    oauth2:
      client:
        registration:
          web-client-oidc:
            provider: spring
            client-id: web-client
            client-secret: secret
            authorization-grant-type: authorization_code
            redirect-uri: "http://127.0.0.1:8760/login/oauth2/code/{registrationId}"
            scope: openid,profile
            client-name: web-client-oidc
          web-client-authorization-code:
            provider: spring
            client-id: web-client
            client-secret: secret
            authorization-grant-type: authorization_code
            redirect-uri: "http://127.0.0.1:8760/authorized"
            scope: message.read
            client-name: web-client-authorization-code
        provider:
          spring:
            issuer-uri: http://localhost:8761

app:
  base-uri: http://127.0.0.1:5173