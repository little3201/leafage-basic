server:
  port: 8768

spring:
  application:
    name: hypervisor

  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  metrics:
    tags:
      application: ${spring.application.name}