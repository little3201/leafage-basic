server:
  port: 8763

spring:
  application:
    name: hypervisor

  profiles:
    active: dev

  web:
    resources:
      add-mappings: false

management:
  endpoints:
    web:
      exposure:
        include: 'prometheus'
  metrics:
    tags:
      application: ${spring.application.name}